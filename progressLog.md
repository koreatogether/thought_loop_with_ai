# Progress Log - ThoughtBloom

## 2026-03-14
- 세션 관리 시스템 구현:
    - 백엔드: `sessions.json`을 활용한 세션 저장, 목록 불러오기, 개별 불러오기 API 구현.
    - 프론트엔드: 헤더에 '저장' 버튼 추가, 첫 화면에 '과거의 생각들 불러오기' 섹션 구현.
    - 자동 저장: '🏠 저장 후 목록으로' 버튼을 통해 종료 시 상태 자동 보존 로직 추가.
    - AI 요약: 저장된 세션 리스트에 AI가 생성한 주제 요약을 표시하여 가시성 확보.
- 레거시 데이터 복원: 기존 `thoughts.json` 데이터를 새 세션 형식으로 변환하는 마이그레이션 도구 실행.
- UI/UX 고도화: 
    - 세션 목록 새로고침 버튼 추가.
    - 인트로 화면 레이아웃 조정 및 세션 목록 가시성 개선.
- 대화 기록 시스템 분리: 
    - 실시간 대화 내역은 `chat_history.json`에 저장.
    - 프로젝트 개발 진척도는 `progressLog.md`에 수동 기록.



## 2026-03-13
- 프로젝트 초기화: 간단한 생각에서 살을 붙여 확장하는 '생각 루프' 컨셉의 웹 앱 기획.
- 기본 구조 구현: `index.html`, `style.css`, `app.js` 생성.
- 디자인 시스템 적용: 다크 모드, 글래스모피즘, 프리미엄 애니메이션.
- 핵심 기능 고도화 (Force-Directed Graph) 및 안정화:
    - 물리 엔진 안정성 강화: 최대 속도 제한(Max Speed) 및 최소 거리 보정(Epsilon)을 통해 노드 이탈 현상 해결.
    - 수동 제어 추가: 노드를 직접 드래그하여 위치를 조정할 수 있는 기능 구현 (드래그 시 해당 노드 물리 연산 일시 정지).
    - 동적 살 붙이기 (Flesh Sizing): 입력하는 글자 수에 비례하여 노드의 가로/세로 크기가 유기적으로 변화.
    - 입력 최적화: 텍스트 입력 시 레이아웃 흔들림 방지를 위해 CSS Transition 최적화.
- 시각 요소 추가: AI 생성 배경 이미지(`bg.png`) 및 더 얇고 세련된 연결선 스타일 적용.
